{
  "name": "ikariam-builder",
  "version": "5.0.0",
  "description": "Chrome Extension — Ikariam automated builder",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:ui": "vitest --ui",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^1.6.0",
    "jsdom": "^29.0.0",
    "vitest": "^1.6.0"
  },
  "dependencies": {
    "playwright": "^1.58.2"
  }
}
